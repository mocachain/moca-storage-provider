name: BlockSyncer E2E Test

# Disabled: mock chain server doesn't support consensus init required by
# NewGfSpBaseApp. Needs proper mock RPC or test refactor to work.
# Re-enable once the mock chain is updated.
on:
  workflow_dispatch:

jobs:
  e2e-test:
    strategy:
      matrix:
        go-version: [ 1.23.x ]
        os: [ ubuntu-latest ]
    runs-on: ubuntu-latest

    steps:
      - name: Install Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go-version }}
      # Checkout Moca-SP
      - name: Checkout Moca-SP
        uses: actions/checkout@v4
      # Run MySQL
      - name: Run MySQL
        run: |
          sudo /etc/init.d/mysql start
      # Build SP and Make Config.toml
      - name: Build and Make Config
        run: |
          bash ./modular/blocksyncer/test/bs_test.sh --makecfg
      # Reset env
      - name: Reset env
        run: |
          bash ./modular/blocksyncer/test/bs_test.sh --reset
      # Run Test
      - name: Run Test
        run: |
          bash ./modular/blocksyncer/test/bs_test.sh --start_test
